{
  "name": "conlang-generator",
  "version": "1.0.0",
  "description": "A CLI to generate a base for a conlang.",
  "main": "build/index.js",
  "scripts": {
    "start": "tsc & node build/index.js",
    "start:shell": "tsc && node build/index.js",
    "init": "npm i prompt-sync yargs @types/node"
  },
  "author": "Lucia",
  "license": "ISC",
  "dependencies": {
    "prompt-sync": "^4.2.0",
    "yargs": "^17.6.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.0"
  }
}
